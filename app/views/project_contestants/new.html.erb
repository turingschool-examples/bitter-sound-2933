<h1>Add Contestant To <%= "#{@project.name}" %></h1>
<%= form_with url:"/projects/#{@project.id}/contestants", method: :post, local: true do |f| %>
    <%= f.label :add_new, 'Insert Contestant Id:' %>
    <%= f.text_field :add_new %>
    <%= f.submit 'Add Contestant To Project' %>
<% end %>
